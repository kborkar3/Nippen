/*package com.projectwork.form;

import javax.servlet.http.HttpServletRequest;

import org.apache.struts.action.ActionErrors;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionMapping;
import org.apache.struts.action.ActionMessage;

//import com.projectwork.impl.LoginServiceImpl;

public class GraphForm extends ActionSupport implements TestProjectConstantsIfc, ServletRequestAwareForm
{
    private static final long serialVersionUID = 1L;

    private String feeder;

    private String startDate;

    private String endDate;

    public String getStartDate()
    {
        return startDate;
    }

    public void setStartDate(String startDate)
    {
        this.startDate = startDate;
    }

    public String getEndDate()
    {
        return endDate;
    }

    public void setEndDate(String endDate)
    {
        this.endDate = endDate;
    }

    public String getFeeder()
    {
        return feeder;
    }

    public void setFeeder(String feeder)
    {
        this.feeder = feeder;
    }

    public ActionErrors validate(ActionMapping mapping, HttpServletRequest request)
    {
        ActionErrors errors = new ActionErrors();

        if (isEmpty(startDate))
            errors.add("startDate", new ActionMessage("error.required", startDate));
        
        if (isEmpty(endDate))
            errors.add("endDate", new ActionMessage("error.required", endDate));
        
        return errors;
    }
    
    protected boolean isEmpty(String value)
    {
        if (value == null || value.trim().equals(""))
        {
            return true;
        }
        return false;
    }

}
*/